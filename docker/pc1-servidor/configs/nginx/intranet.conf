server {
    listen [::]:80;
    listen 80;
    server_name intranet.dominio.local dominio.local;
    root /var/www/intranet;
    index index.html;
    access_log /var/log/nginx/intranet.access.log;
    error_log  /var/log/nginx/intranet.error.log;
    location / { try_files $uri $uri/ =404; }
}
